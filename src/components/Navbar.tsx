import React from 'react';
import { Container } from 'react-bootstrap';

export const Navbar = () => {
    
    return (
        <Container>
            <nav>
                clark | kent | tylers
            </nav>
        </Container>
    );
    
}